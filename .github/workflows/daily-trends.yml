name: Daily Trend Collection

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  collect_trends:
    runs-on: ubuntu-latest

    steps:
    - name: Call YouTube Trends Collector
      run: |
        curl -X POST "${{ secrets.TRENDS_URL }}/api/trends/youtube"
